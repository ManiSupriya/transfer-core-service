trigger:
  - master
  - develop

parameters:
 VeracodeAppName: 'ubtablet'

pr:
  - master
  - develop

resources:
  repositories:
    - repository: templates
      type: git
      name: Core/build-templates

stages:
  - stage: Build
    displayName: Build
    jobs:
      - template: job-build-springboot-jar.yaml@templates